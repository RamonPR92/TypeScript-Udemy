"use strict";
(() => {
    //Void indica que no hay retorno
    //funciona como en java donde se indica que no hay retorno en la funcion
    function callBatman() {
    }
    const a = callBatman();
    console.log(a);
})();
